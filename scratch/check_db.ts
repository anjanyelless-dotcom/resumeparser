import { getClient } from '../backend/src/database/db'; async function check() { const c = await getClient(); try { const u = await c.query('SELECT column_name FROM information_schema.columns WHERE table_name = ''users'''); console.log('Users columns:', u.rows.map(r=>r.column_name)); const c_table = await c.query('SELECT column_name FROM information_schema.columns WHERE table_name = ''candidates'''); console.log('Candidates columns:', c_table.rows.map(r=>r.column_name)); } finally { c.release(); process.exit(0); } } check();
